spring:
  datasource:
    url: jdbc:postgresql://localhost:${DOCKER_DB_PORT:5432}/${DOCKER_DB_NAME:taller_TIS}
    username: ${DOCKER_DB_USER:postgres}
    password: ${DOCKER_DB_PASSWORD:dfigueroa}
    driver-class-name: org.postgresql.Driver
